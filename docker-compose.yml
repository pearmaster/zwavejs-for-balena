version: '2.1'
volumes:
  zwavejs2mqtt: {}
services:
  main:
    image: "zwavejs/zwavejs2mqtt:latest"
    ports:
      - "3000:3000"
      - "8091:8091"
    volumes:
      - "zwavejs2mqtt:/usr/src/app/store"
    privileged: true
    restart: always
    network_mode: host